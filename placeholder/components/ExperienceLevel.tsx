// import { useState } from 'react';
// import ProgressBar from './ProgressBar';
// import {useColorMode} from '@chakra-ui/react'

// function ExperienceLevel({level, descriptions, progressValue}: {level: string, descriptions: string, progressValue: number }) {

//   const [level, setLevel] = useState('')


//   function handleLevel(event: React.MouseEvent<HTMLButtonElement>) {
//     event.preventDefault();
//     setLevel(event.currentTarget.value);

//   }
// }